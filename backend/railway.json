{
    "build": {
      "env": {
        "PYTHONUNBUFFERED": "1"
      },
      "builder": "Docker",
      "dockerfilePath": "backend/Dockerfile"
    },
    "deploy": {
      "startCommand": "gunicorn backend.server:app -b 0.0.0.0:$PORT"
    }
  }